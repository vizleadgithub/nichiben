# Ph.11 修正レポート — CI3 CSRF保護の有効化

**対象脆弱性分類**: F-10  
**修正日**: 2026-06-26  
**更新日**: 2026-06-30（alflearning-api の設定を FALSE に確定）  
**修正ファイル数**: 1ファイル（alflearning-api は対象外と確定）

---

## 修正内容（確定版）

| ファイル | 行 | 修正前 | 修正後 | 適用 |
|---|---|---|---|---|
| `alflearning/alflearning-cms/application/config/config.php` | 469 | `$config['csrf_protection'] = false;` | `$config['csrf_protection'] = TRUE;` | ✅ 適用 |
| `alflearning/alflearning-api/application/config/config.php` | 468 | `$config['csrf_protection'] = FALSE;` | （変更なし — `FALSE` のまま維持） | ❌ 非適用（後述） |

---

## alflearning-api を FALSE のまま維持する理由

### 問題

`alflearning-api` に `csrf_protection = TRUE` を適用したところ、SSO ログイン（`Openam_sso` コントローラー経由）が機能しなくなった。SSO プロバイダーから API エンドポイントへの POST コールバックには CSRF トークンが含まれないため、CI3 が 403 で拒否する。

### 判断根拠

**REST API に CSRF 保護は本来不要である。**

CSRF 攻撃が成立するには「ブラウザが被害者のセッション Cookie を自動送信する」という前提が必要である。alflearning-api への POST は以下のいずれかであり、いずれもこの前提を満たさない：

| 呼び出し元 | 方式 | ブラウザの Cookie 自動送信 |
|---|---|---|
| alfproduct（PHP コード） | サーバー間 curl | なし |
| SSO プロバイダー（Openam 等） | サーバー間 POST コールバック | なし |
| CI3 バッチ処理 | CLI / curl | なし |

加えて alflearning-api は HTTP Basic 認証で保護されており、CSRF 攻撃の前提となる「認証情報なしでのリクエスト成功」が成立しない。

**結論: `alflearning-api` の `csrf_protection` は `FALSE` のままとする。これは脆弱性ではなく REST API として正しい設計である。**

---

## alflearning-cms の設定

管理画面の全 POST フォームに CSRF トークン検証が適用される。CI3 の CSRF 保護は：
1. レスポンス時に Cookie (`csrf_cookie_name`) にトークンを設定する
2. POST リクエスト受信時に Cookie のトークンと POST パラメータ (`csrf_token_name`) を照合する
3. 不一致の場合 403 を返す

CMS フォームは既に CI3 の `form_open()` または `<?php echo form_open('...') ?>` を使っていればトークンが自動挿入される。ただし **生の `<form>` タグを使っている箇所**は手動で hidden フィールドを追加する必要がある。

現在の設定値：
- `csrf_token_name` = `csrf_test_name`
- `csrf_cookie_name` = `csrf_cookie_name`
- `csrf_expire` = 7200 秒（2時間）
- `csrf_regenerate` = FALSE（Ph.20 で変更 — マルチタブ操作によるトークン不一致を防ぐため）
